import { ContentLayout } from "@/components/Layout";

export const Definitions = () => {
    return (
        <ContentLayout title="Definitions">
        <h1>Definitions</h1>
        </ContentLayout>

    );
}